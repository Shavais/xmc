// numberic literals, variables and math

// integer variables and math: +, -, *, /, ^
u8 a = 1024;                        // 
u16 b = a / 2;                      // 
u32 c = a + b;
u64 d = c * ((a * b - b * c));      // netagive; wraps around to a very high positive
u8 e = d % 13;

f32 pi = 3.1415962;
f64 g = 2 * pi * pi / 3;    // when floats

d8 = 

test1()
{
    SVGE_Log(a)
}

namespace 